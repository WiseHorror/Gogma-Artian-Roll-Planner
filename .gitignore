# Ignore everything by default
*

# But keep the repository root files and the GARP.lua script
!/.gitignore
!/.gitattributes
!/.gitmodules
!/.github/
!README.md
!AGENTS.md

# Keep the active REFramework script and ignore everything else below it.
!reframework/
reframework/*
!reframework/autorun/
reframework/autorun/*
!reframework/autorun/GARP.lua

# Keep any .txt probe outputs (optional)
# !reframework/data/Gog Roll Planner/SelectedWeaponProbe.txt
